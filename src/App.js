import React, { Component } from 'react';
import CustomButton from './CustomButton';

export default class App extends Component {
  render() {
    return (
      <h1>Hello, world.<CustomButton /></h1>
    );
  }
}
